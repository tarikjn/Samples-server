%h1 Listing campaigns

%table
  %tr
    %th Brand owner
    %th Product name
    %th Barcode
    %th Small image
    %th Splash image
    %th Active
    %th
    %th
    %th

  - @campaigns.each do |campaign|
    %tr
      %td= campaign.brand_owner
      %td= campaign.product_name
      %td= campaign.barcode
      %td= campaign.small_image
      %td= campaign.splash_image
      %td= campaign.active
      %td= link_to 'Show', campaign
      %td= link_to 'Edit', edit_campaign_path(campaign)
      %td= link_to 'Destroy', campaign, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Campaign', new_campaign_path
